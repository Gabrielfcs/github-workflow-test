name: Test Workflow

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run test
      run: |
        echo "Testing GitHub Actions notifications!"
        echo "This workflow should trigger a notification."
        sleep 5
        echo "Test completed successfully!"

    - name: Success
      run: echo "✅ All tests passed!"
